# Face Authentication Attendance System
# Python 3.8+

# Web framework
flask>=2.3.0
flask-cors>=4.0.0

# Computer vision & face recognition
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Pre-built dlib for Windows (no CMake). On Linux/Mac: pip install dlib
dlib-bin>=19.24.0; sys_platform == "win32"
dlib>=19.7; sys_platform != "win32"

# Face recognition (requires dlib). On Windows after dlib-bin: pip install face-recognition --no-deps
face-recognition>=1.3.0
